export type ThemeVariables = {
	infoColor: string;
	errorColor: string;
	warningColor: string;
	successColor: string;
	darkColor: string;
	lightColor: string;
	disableColor: string;
};
export type ThemeVariableKeys = keyof ThemeVariables;
